{
  "name": "is-express-router",
  "version": "1.0.0",
  "description": "Checking if an argument is an express Router function from express.Router() (like router.get(), router.post(), ..)",
  "keywords": [
    "express",
    "router",
    "is express router",
    "express.Router()"
  ],
  "main": "index.js",
  "repository": "https://github.com/matt-hahn/is-express-router.git",
  "author": "Matt Hahn <matt.hahn@matthahndesign.com>",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "babel-eslint": "^8.2.1",
    "chai": "^4.1.2",
    "eslint": "^4.15.0",
    "eslint-config-prettier": "^2.9.0",
    "eslint-plugin-prettier": "^2.4.0",
    "express": "^4.16.2",
    "mocha": "^4.1.0",
    "prettier": "^1.10.2"
  },
  "scripts": {
    "test": "NODE_ENV=test mocha src/**/**.test.js",
    "test:watch": "NODE_ENV=test mocha --watch --recursive src/**/**.test.js",
    "test:lint": "npm run eslint && npm test",
    "eslint": "eslint src/**"
  },
  "dependencies": {
    "express-router-methods": "^1.0.2",
    "is-function": "^1.0.1"
  }
}
